diff --git a/mobile/src/components/ChatInput.js b/mobile/src/components/ChatInput.js
index e03e73f..3d37287 100644
--- a/mobile/src/components/ChatInput.js
+++ b/mobile/src/components/ChatInput.js
@@ -19,7 +19,10 @@ const SPRING_CONFIG = {
   mass: 0.8        // Lower = lighter/faster (try 0.5-2)
 };
 
-function ChatInputComponent({ onSend, isStreaming, onStop, placeholder = 'Ask anything', value = '', onChangeText, onOpenAttachmentModal, onAttachmentsChange, onInputHeightChange }, ref) {
+// Height of agentic mode pill section
+const AGENTIC_SECTION_HEIGHT = 36;
+
+function ChatInputComponent({ onSend, isStreaming, onStop, placeholder = 'Ask anything', value = '', onChangeText, onOpenAttachmentModal, onAttachmentsChange, onInputHeightChange, agenticMode, onToggleAgenticMode }, ref) {
   const [text, setText] = useState(value || '');
   const [keyboardVisible, setKeyboardVisible] = useState(false);
   const [attachments, setAttachments] = useState([]);
@@ -31,6 +34,8 @@ function ChatInputComponent({ onSend, isStreaming, onStop, placeholder = 'Ask an
   
   // Animation value for attachment section height
   const attachmentSectionHeight = useSharedValue(0);
+  // Animation value for agentic mode pill
+  const agenticSectionHeight = useSharedValue(0);
 
   // Track keyboard visibility
   useEffect(() => {
@@ -89,12 +94,23 @@ function ChatInputComponent({ onSend, isStreaming, onStop, placeholder = 'Ask an
     onAttachmentsChange?.(attachments.length);
   }, [attachments.length, onAttachmentsChange]);
 
+  // Animate agentic section height
+  useEffect(() => {
+    agenticSectionHeight.value = withSpring(agenticMode ? AGENTIC_SECTION_HEIGHT : 0, SPRING_CONFIG);
+  }, [agenticMode]);
+
   const attachmentStyle = useAnimatedStyle(() => ({
     height: attachmentSectionHeight.value,
     opacity: attachmentSectionHeight.value > 10 ? 1 : 0, // Prevent flicker at 0
     overflow: 'hidden',
   }));
 
+  const agenticStyle = useAnimatedStyle(() => ({
+    height: agenticSectionHeight.value,
+    opacity: agenticSectionHeight.value > 10 ? 1 : 0,
+    overflow: 'hidden',
+  }));
+
   // Match gradient height logic below
   // ...
   
@@ -102,13 +118,14 @@ function ChatInputComponent({ onSend, isStreaming, onStop, placeholder = 'Ask an
   const effectiveInputHeight = Math.min(inputHeight, 150);
   const extraInputHeight = baseInputHeight.current > 0 ? Math.max(0, effectiveInputHeight - baseInputHeight.current) : 0;
   const attachmentHeight = attachments.length > 0 ? ATTACHMENT_SECTION_HEIGHT + 5 : 0; // +5 for extra breathing room in gradient
+  const agenticHeight = agenticMode ? AGENTIC_SECTION_HEIGHT : 0;
 
   return (
     <View style={styles.wrapper}>
       <LinearGradient
         colors={['transparent', COLORS.bg70, COLORS.bg90, COLORS.bg90]}
         locations={[0, 0.45, 0.6, 1]}
-        style={[styles.bottomFade, { height: insets.bottom + 85 + extraInputHeight + attachmentHeight }]}
+        style={[styles.bottomFade, { height: insets.bottom + 85 + extraInputHeight + attachmentHeight + agenticHeight }]}
         pointerEvents="none"
       />
       
@@ -121,6 +138,19 @@ function ChatInputComponent({ onSend, isStreaming, onStop, placeholder = 'Ask an
       </Pressable>
 
       <View style={styles.containerInput}>
+        
+        {/* Agentic Mode Pill - Top */}
+        <Reanimated.View style={agenticStyle}>
+          <View style={styles.agenticContainer}>
+            <View style={styles.agenticPill}>
+              <Text style={styles.agenticText}>Agentic Mode</Text>
+              <Pressable onPress={onToggleAgenticMode} hitSlop={5}>
+                <Ionicons name="close-circle" size={16} color={COLORS.primary} style={{ marginLeft: 6 }} />
+              </Pressable>
+            </View>
+          </View>
+        </Reanimated.View>
+
         {/* Attachment preview - above input */}
         <Reanimated.View style={attachmentStyle}>
           <AttachmentPreview 
@@ -212,6 +242,28 @@ const styles = StyleSheet.create({
     paddingVertical: 4,
     zIndex: 100,
   },
+  agenticContainer: {
+    paddingHorizontal: 12,
+    paddingTop: 8,
+    paddingBottom: 2,
+    flexDirection: 'row',
+  },
+  agenticPill: {
+    flexDirection: 'row',
+    alignItems: 'center',
+    backgroundColor: 'rgba(59, 130, 246, 0.15)', // Blue transparent
+    borderWidth: 1,
+    borderColor: 'rgba(59, 130, 246, 0.5)',
+    borderRadius: 20,
+    paddingHorizontal: 10,
+    paddingVertical: 4,
+  },
+  agenticText: {
+    color: COLORS.primary, // Assume primary is blue-ish or adapt
+    fontSize: 11,
+    fontFamily: FONTS.mono,
+    fontWeight: '600',
+  },
   inputRow: {
     flexDirection: 'row',
     alignItems: 'center',
