diff --git a/mobile/src/screens/ModelsListScreen.js b/mobile/src/screens/ModelsListScreen.js
index 003860a..ebe15d5 100644
--- a/mobile/src/screens/ModelsListScreen.js
+++ b/mobile/src/screens/ModelsListScreen.js
@@ -1,5 +1,5 @@
 import { useState } from 'react';
-import { View, Text, TextInput, StyleSheet, ScrollView, Modal, FlatList, TouchableWithoutFeedback, Pressable } from 'react-native';
+import { View, Text, TextInput, StyleSheet, ScrollView, Modal, FlatList, TouchableWithoutFeedback, Pressable, Switch } from 'react-native';
 import { Pressable as GHPressable } from 'react-native-gesture-handler';
 import { Ionicons } from '@expo/vector-icons';
 import { useApp } from '../context/AppContext';
@@ -94,6 +94,7 @@ export default function ModelsListScreen({ onClose, dragHandlers }) {
     model: settings.model || '',
     apiKey: providerApiKeys[settings.provider] || settings.apiKey || '',
     baseUrl: settings.baseUrl || '',
+    agenticMode: settings.agenticMode || false,
   });
   const [showApiKey, setShowApiKey] = useState(false);
   const [contextMenu, setContextMenu] = useState({ visible: false, item: null, type: null, position: { x: 0, y: 0 } });
@@ -136,6 +137,7 @@ export default function ModelsListScreen({ onClose, dragHandlers }) {
       model: localSettings.model,
       baseUrl: localSettings.baseUrl,
       apiKey: localSettings.apiKey, // Keep for backward compatibility
+      agenticMode: localSettings.agenticMode,
     });
     showAlert('success', 'Saved', 'Model settings saved');
   };
@@ -370,6 +372,22 @@ export default function ModelsListScreen({ onClose, dragHandlers }) {
           )}
         </View>
 
+        {/* Agentic Mode */}
+        <View style={styles.section}>
+          <View style={styles.switchRow}>
+            <View>
+              <Text style={styles.sectionTitle}>Agentic Mode</Text>
+              <Text style={styles.switchDescription}>Allow the AI to use tools (Web Search, Image Gen)</Text>
+            </View>
+            <Switch
+              value={localSettings.agenticMode}
+              onValueChange={(val) => setLocalSettings(prev => ({ ...prev, agenticMode: val }))}
+              trackColor={{ false: COLORS.inputBg, true: COLORS.primary }}
+              thumbColor={COLORS.fg}
+            />
+          </View>
+        </View>
+
         <Pressable style={styles.saveBtn} onPress={handleSave} android_ripple={{ color: 'rgba(255,255,255,0.2)' }}>
           <Text style={styles.saveBtnText}>Save Model Settings</Text>
         </Pressable>
@@ -502,4 +520,15 @@ const styles = StyleSheet.create({
     marginTop: 8,
   },
   saveBtnText: { color: COLORS.fg, fontSize: 15, fontFamily: FONTS.display },
+  switchRow: {
+    flexDirection: 'row',
+    alignItems: 'center',
+    justifyContent: 'space-between',
+    backgroundColor: COLORS.inputBg,
+    borderRadius: 15,
+    padding: 14,
+    borderWidth: 1,
+    borderColor: COLORS.borderLight,
+  },
+  switchDescription: { color: COLORS.fgMuted, fontSize: 12, marginTop: 4, maxWidth: '80%' },
 });
